// @ts-check
const { defineConfig } = require('react-showroom');

module.exports = defineConfig({
  title: 'React Showroom',
  items: [
    {
      type: 'docs',
      folder: 'pages',
    },
  ],
});
